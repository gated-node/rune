name: Release

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger release build
        run: echo "Release triggered manually. Build locally with build-release.sh"
